postgresql_databases:
  - name: "{{ morpheus['customOptions']['pgsql_db'] }}" 
postgresql_users:
  - name: "{{ morpheus['customOptions']['pgsql_username'] }}" 
    password: "{{ morpheus['customOptions']['pgsql_password'] }}" 
#ansible_become_password: "{{ lookup('cypher','secret=password/becomepass') }}"
